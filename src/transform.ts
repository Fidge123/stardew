export class SaveGame {
  // private file: any;

  constructor(path: string) {
    this.read(path);
  }

  // public transform(remove: string, add: string): boolean {
  //   this.file = this.file;
  //   return true;
  // }

  // public write(path: string): boolean {
  //   return true;
  // }

  private read(path: string): any {
    // this.file = path;
    return path;
  }

}
